# Normalize line endings to LF in the repository, matching .editorconfig's
# `end_of_line = lf`. Without this, behaviour depends on each contributor's
# local core.autocrlf setting, which produces spurious whole-file diffs and
# "LF will be replaced by CRLF" warnings on Windows checkouts.
* text=auto eol=lf
